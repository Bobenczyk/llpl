/// comment

/// next thing to do "string" YESS SIRRr
/// also unlist (list -> stack) where list (stack -> list) yes sir :D


/// {
/// (list (list abc {);})
/// }
/// }


(set a {
    (print 1 2 3 = [1 2 3 +])
})

(printtype (get a))
(do (get a))
(do (get a))

/// (print (concat (get args) (get SPACE) ) )